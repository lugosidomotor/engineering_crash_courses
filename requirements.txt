# Engineering Crash Courses — egyesített Python függőségek (2026 május)
#
# ⚠️  PYTHON 3.11 KÖTELEZŐ
# A kurzusokat Python 3.11-en teszteltük. Az `apache-airflow==2.10.3` és a
# `feast==0.41.3` build problémái miatt 3.12+ nem támogatott, 3.13+ pedig
# nem fog menni az Airflow-val. Ha 3.13-on dolgozol, telepítsd a 3.11-et:
#   - macOS:  brew install python@3.11
#   - Linux:  apt/dnf install python3.11
#   - Windows: https://www.python.org/downloads/release/python-3119/
#
# Telepítés:  python3.11 -m venv .venv && .venv\Scripts\activate (Windows)
#             vagy source .venv/bin/activate (Linux/Mac)
#             pip install -r requirements.txt
#
# Nem minden csomag kell minden kurzushoz — telepítheted szelektíven is.
#
# Tesztelt környezet:  Python 3.11.9 + pip 24.x

# Core data
pandas==2.2.3
pyarrow==18.1.0
numpy==2.1.3
duckdb==1.1.3
pydantic==2.10.4

# IO / DB / API
psycopg[binary]==3.2.3
sqlalchemy==2.0.36
requests==2.32.3
pytest==8.3.4

# Lakehouse / formats
deltalake==0.22.3
pyiceberg==0.8.1

# Spark
pyspark==3.5.3

# Streaming
kafka-python==2.0.2
confluent-kafka==2.6.1

# Cloud / object storage
boto3==1.35.84

# Orchestration / transformation
apache-airflow==2.10.3
dbt-postgres==1.9.0

# Quality
great-expectations==0.18.22

# ML / LLM ops
mlflow==2.20.3
scikit-learn==1.6.0
fastapi==0.115.6
uvicorn[standard]==0.34.0
prometheus-client==0.21.1

# Feature store
feast==0.41.3

# RAG / vector DB
chromadb==0.5.23
sentence-transformers==3.3.1
openai==1.59.7
anthropic==0.42.0
ragas==0.2.10
